class Solution {
    public double myPow(double x, int n) {
        // double result = 1.0;
        // long new_n = n;
        // if(new_n < 0)
        //     new_n = -1 * n;
        // for(int  i = 0; i < n; i++)
        // {   
             
        //     result = result * x;
           
        // }
       double result =  Math.pow(x,n);
        return result;
    }
}